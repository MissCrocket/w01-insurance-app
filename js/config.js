// js/config.js
export const STORAGE_KEY = 'cii_w01_tutor_progress';

// --- NEW ---
// Feature flag for the question flagging feature
export const FEATURE_FLAG_QUESTION_FLAGGING = true;
// Settings for the flagging feature (as specified in the spec)
export const FLAGGING_CONFIG = {
  enableWarningOnSubmit: true,
};
// --- END NEW ---